<!--
Fill this once per feature/session. Save as a reusable context file and pass it into tools so they can skip already-answered questions.
Example: /risk-analysis .testing/context-login.md
-->

feature:

description:

jira_ticket:

epic:

user_story:

acceptance_criteria:

platform:

users:

goal:

dependencies:

known_bugs:

constraints:

specs_or_docs:

notes:
